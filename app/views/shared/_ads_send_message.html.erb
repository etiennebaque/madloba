<!-- 'Send message' text area -->
<% if !is_owning(ad) %>
    <div class="col-lg-12 ad-paragraph" xmlns="http://www.w3.org/1999/html">
      <!-- Send message to the ad publisher -->
      <%= form_tag '/ads/send_message', {class:'form-horizontal'} do %>
          <%= hidden_field_tag 'id', ad.id %>


          <fieldset>

            <div class="form-group" style="margin-left: 0px;">
              <%= label_tag :title, t('ad.send_message_label', name: publisher_name(ad)), class: 'control-label' %>
            </div>


            <% if !user_signed_in? %>
                <div class="form-group">
                  <%= label_tag 'name', t('ad.name'), class: 'col-lg-2 control-label' %>
                  <div class="col-lg-4">
                    <%= text_field_tag 'name', params['name'], class: 'form-control', required: true %>
                  </div>
                </div>

                <div class="form-group">
                  <%= label_tag 'email', t('ad.your_email')+'*:', class: 'col-lg-2 control-label' %>
                  <div class="col-lg-4">
                    <%= email_field_tag 'email', params['email'], class: 'form-control', required: true %>
                  </div>
                </div>

                <!-- Captcha for anonymous user -->
                <div class="form-group">
                  <%= label_tag 'captcha', t('ad.captcha'), class: 'col-lg-2 control-label' %>
                  <div class="col-lg-5">
                    <%= show_simple_captcha %>
                  </div>
                </div>

            <% end %>

            <div class="form-group">
              <%= label_tag 'message', t('ad.your_message'), class: 'col-lg-2 control-label' %>
              <div class="col-lg-6">
                <%= text_area_tag 'message', params['message'], rows: 8, class: 'form-control', style: 'padding-bottom: 20px;' %>
              </div>
            </div>

          </fieldset>

          <div class="form-actions">
            <div class="col-lg-8" style="text-align: right;">
                <%= submit_tag(t('ad.send_message'), class: 'btn btn-success') %>
            </div>
          </div>
      <% end %>
    </div>
<% end %>